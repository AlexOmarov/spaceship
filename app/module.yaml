# app/module.yaml
product: jvm/app

dependencies:
  - ../api

settings:
  kotlin:
    languageVersion: 2.3
  jvm:
    jdk:
      version: 25  # major JDK version
      distributions: [liberica]  # optional allowlist of distributions (accept all if omitted)